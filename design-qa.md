# 工作台视觉验收记录

final result: passed

范围：选定效果图对应的工作台外壳与模型配置页，以及新导航在窄屏下的可用性。此结果为工程自验，不代表用户已作出视觉验收，也不包括真实模型生成。

## 比较对象与状态

- Source visual truth：`.workbench-qa/参考效果图.png`，保留选定效果图的原始副本。
- Implementation：`http://127.0.0.1:8002/`；截图 `.workbench-qa/工作台-桌面.png`。
- Source pixels：1586 × 992。Implementation pixels / CSS viewport：1586 × 992，device scale factor 1。
- 第二轮并排对照：`.workbench-qa/对照-第二轮.png`，3172 × 1032；左右原图各 1586 × 992，顶部各增加 40px 标题。未拉伸、未裁剪；以同一比较图检查全部页面。
- 状态：浅色、多步协作、选中剧情规划、高推理、2000 tokens；高级参数折叠。
- 内容差异已区分：参考为「第四天灾 / 平衡方案」示例四步；实现为隔离数据目录中的真实默认方案与示例故事 Seraphina。真实方案有五个预处理阶段、七个节点，随后正文生成。没有伪造四步、GPT-5 连接或用户故事数据。

## 发现与修正

第一轮：`.workbench-qa/对照-第一轮.png`。状态为 blocked。

1. **P2：旧导航图标叠在工作台顶部。** 原背景条与 `top-settings-holder` 是两个独立元素；工作台模式现在同时隐藏，进入原生界面时恢复。第二轮顶部只保留工作台自己的标题与故事入口。
2. **P2：控件字号、说明文字和主按钮偏小。** 调整标签至 20px、说明至 17px，并将保存按钮最小宽度调整至 172px。第二轮已逐项对照。
3. **P1：窄屏下工作区高度坍缩。** 平板实测主区域仅 52px 高；原页面的移动端根变换形成了高度为零的定位容器。工作台改为显式动态视口高度，手机底栏采用显式视口位置。修正后平板主区域高度 1024px、手机主区域高度 771px。
4. **P2：平板双栏最小宽度可能超出可用空间。** 缩小中等宽度下的列最小宽度；最终平板主区域 clientWidth / scrollWidth 同为 682px，手机同为 390px。
5. **P2：重新渲染会把后段选中节点滚出视野。** 保留列表滚动位置并确保选中节点可见；实际选择最后一个预处理节点后验证通过。

第二轮：已重新捕获桌面与窄屏界面并检查以上修正，无待修复的 P0 / P1 / P2 项。

## 必查维度

| 维度 | 结论 |
| --- | --- |
| 字体与层级 | Windows 实际渲染为 Microsoft YaHei UI / Microsoft YaHei，保留 Noto Sans 回退。标题 40px，卡片标题 30px，主要字段标签 20px。参考是生成图片，未假称已识别其原始字体文件。 |
| 布局与间距 | 保留左侧固定导航、顶部模式切换、约 42:58 的双栏和卡片内对齐。真实节点数量增加时在流程列表内滚动；选中节点保持可见。 |
| 颜色与标记 | 暖白背景、白卡、灰色细边框、深绿选中态与主按钮、浅绿提示；无旧工具栏覆盖。 |
| 资产质量 | 使用项目已有 Font Awesome 图标和真实故事缩略图；没有手绘 SVG、占位图或假模型资产。与参考的线性图标相比，实心图标是已接受的 P3 差异。 |
| 文案与配置真相 | 全局方案、共享预设、真实连接模型、继承默认和保存反馈均如实标明；没有把设计图中的虚构样例写进运行状态。 |
| 响应式与可操作性 | 手机底栏四个入口可见，配置表单可滚动到保存按钮；保存按钮底部约 728px，底栏顶端 771px。真实导航、表单及错误状态均执行检查。 |

## 窄屏证据

- 修正前：`.workbench-qa/工作台-平板-检查.png`。
- 平板：`.workbench-qa/工作台-平板.png`，768 × 1024。
- 手机：`.workbench-qa/工作台-手机.png`，390 × 844，滚动至配置表单与保存区。

## 剩余范围

- P3：图标笔画与生成图片略有不同；未另加图标依赖或仿画资产。
- 原生聊天、完整高级设置沿用 Luker 界面；本次的参考图未定义它们的新视觉方案。
- 真实 Provider 输出、用户原数据迁移及最终人工视觉验收未执行。
