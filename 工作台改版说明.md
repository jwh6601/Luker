# Luker 故事工作台

基于 Luker 2.7.0 / `239b329ea848d2d80abcac53c16f3f84fa9a9052`，按选定效果图实现浅色工作台。保留 Luker 原有宿主、生成流程、扩展和数据格式。

## 打开与运行

当前独立预览地址：<http://127.0.0.1:8002/>。

Windows 下安装 Node.js 22.19 或更高版本和项目依赖后，双击 `启动工作台.cmd`。命令行也可以运行：

```sh
npm install
node server.js --port 8002 --listen false --browserLaunchEnabled false
```

本地工作目录为 `D:/CodexData/workspaces/luker-workbench`。预览使用该目录下独立的 `data/`，没有迁入原安装版的聊天、连接凭据或扩展数据。8001 端口上的原安装版可以继续使用。

## 已实现

- 四个主导航入口：故事库、当前故事、模型与流程、设置。
- 故事库搜索与故事选择，调用原有角色选择和聊天功能；导入、新建和完整设置使用原有入口。
- 「模型与流程」双栏界面：读取真实 Spec 方案的阶段、并行节点与参数，不把效果图里的示例四步写成固定流程。
- 每个环节分别选择已有 API 连接、模型和参数预设；推理等级和最大输出长度可以单独调整。
- 修改推理等级或输出长度时，通过 Luker 原有 API 创建独立参数预设，再绑定到对应环节。正文当前配置和其他环节的参数不会因此被修改。为避免覆盖共享参数，每次此类保存都会新增一份预设；历史副本可在原预设管理器中管理。
- 保存使用原有方案库与设置接口，并重新读取保存结果确认成功；提供未保存提醒、输入校验、失败反馈与保存期间的编辑保护。
- 适配桌面、平板与手机；手机使用底部导航。

这里编辑的是**全局 Spec 方案**。故事专属覆盖会明确提示；其他流程类型和完整编辑功能通过原有编辑器使用。多个节点共用同一环节预设时，会提示共享影响。原生聊天渲染和完整高级设置保留原实现，本次没有重新设计这些复杂界面。

工作台随内置 Orchestrator 扩展加载；「直接生成」只关闭多步处理，不关闭扩展本身。在扩展管理中停用该扩展后，会回到原有界面。

## 验证

- 新增逻辑测试：4 项通过，验证拓扑与其他环节保留、拒绝越界写入、自定义节点标识、参数范围校验。
- 真实浏览器操作：保存、重载、参数隔离、连接选择、无效输入、保存失败、故事搜索与选择、设置入口、完整流程编辑器、后段节点滚动可见性均已验证。
- 桌面 `1586 × 992`、平板 `768 × 1024`、手机 `390 × 844` 检查通过；平板和手机主区域没有横向溢出，手机保存按钮不被底部导航遮挡。
- 新增 JS 文件 ESLint 通过。上游原有 `main.js` 有 5 个未使用符号错误；原有插件边界检查有 12 项违规。本次没有把这些既有问题标成通过，也没有顺带修改它们。
- 视觉对照见 `design-qa.md`；本机截图与验证产物位于 `.workbench-qa/`，不提交到 Git。
- 没有调用真实模型生成，也没有迁移或验证原安装版的真实用户数据。

复跑直接相关逻辑测试：

```sh
node --test tests/frontend/workbench-state.test.mjs
node node_modules/eslint/bin/eslint.js public/scripts/extensions/orchestrator/workbench.js public/scripts/extensions/orchestrator/workbench-state.js
```

## 保存连接与 Codex 订阅（2026-09-11）

在「模型与流程 → 管理连接」或「设置 → 管理连接」中添加连接。支持 OpenAI 兼容 API、OpenAI、DeepSeek、Claude 和 Codex 订阅。连接管理只保存名称、地址、密钥或订阅登录，不再选择模型。各 pipeline 环节分别选择连接和模型，并随方案保存；正文生成分别选择连接和模型后，点击“应用正文连接与模型”。同一连接可供多个环节使用不同模型。旧连接中已有的模型值只作为兼容默认值保留，新建连接不写入模型。已有连接名称保持不变，避免破坏按名称引用的环节配置；其他高级连接类型仍可使用原有 API 抽屉编辑。

Codex 复用 DSH `llm-pi-ai` 的 OAuth 交互与凭据读写适配方式，依赖锁定同版本 `@earendil-works/pi-ai@0.82.1`。这是 DSH 的 pi-ai 订阅模型路线，不是 Codex App Server 子代理。相关许可保存在 `docs/DSH代码复用许可.txt`。登录由用户在浏览器完成，支持自动回调、手动回调、取消和五分钟超时；凭据保存在当前 Luker 用户的既有 secrets 文件，串行刷新，不复制其他应用的登录文件。多个 Codex 连接共用当前 Luker 用户的一份订阅登录。

服务器在既有生成分发器中适配文本、工具调用、推理文本、非流式结果和流式结果。连接内部使用 `https://codex.luker.invalid` 作为路由标识，命中后由专用适配器处理，不向该地址发送网络请求。模型下拉框显示锁定版本目录的所有候选，不再使用会按当前输入过滤的 datalist；这不是账号权限查询，未宣称覆盖 Pro 20× 的全部可用模型。模型列表来自锁定版本的供应商目录；当前不支持此入口的图片/音频消息。Codex 输出上限由订阅服务决定，JSON schema 目前转换为提示词约束，不宣称服务端严格结构化输出。

本批验证：12 项直接相关测试通过；真实浏览器验证了连接保存与重载、环节解析、正文连接应用、保存失败回滚、Codex 登录链接与取消、桌面与手机布局。普通 API 经本机模拟服务完整请求，确认所选地址、模型和密钥一致。临时测试连接和测试密钥已移除。新增及直接修改的工作台/后端文件 ESLint 通过；原有 Connection Manager 文件仍有 2 项既有 lint 错误，本批只导出其既有函数，未改变这两处。模型分离批次的 generate-task.js 另有 3 项既有 lint 错误，spec-runtime.js 有 1 项既有 lint 错误；新增的模型传递未修改这些原有行。订阅登录现已由用户完成（页面显示已登录），真实模型生成仍为 `NOT_OBSERVED`。

复跑：`node --test tests/codex-subscription.test.mjs tests/frontend/workbench-state.test.mjs`。

## 回退

改版和原安装版独立运行。关闭此预览后仍可使用原安装版。源码回退可在保存本地改动后切换回上游 `release` 分支；无需修改或删除原安装版的数据。
